# IDEs
.idea/

# Hugo
resources/
public/
jsconfig.json
node_modules/
go.sum
.hugo_build.lock

# Hugo binaries and archives (large files)
hugo
hugo.tar.gz
hugo_extended.tar.gz

# macOS
.DS_Store

# Hugo build output (now built by GitHub Actions, not committed)
docs/
